defaults:
  - data: default
  - model: fno2d
  - training: default
  - loss: default
  - eval: default
  - _self_

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  job:
    chdir: true

wandb:
  project: fno-rollout-divergence
  entity: null
  mode: offline
  tags: []

seed: 42
device: cuda
